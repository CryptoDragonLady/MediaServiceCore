package com.liskovsoft.youtubeapi.app.potokencloud

// Add trusted legacy provider endpoints here when one is explicitly configured.
// Upstream's service1.com/service2.com values were placeholders, not PO-token services.
internal val PO_TOKEN_CLOUD_BASE_URLS = emptyArray<String>()
